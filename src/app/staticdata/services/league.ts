export interface TeamLeagueDTO {
  sport: string;
  teamCode?: string;
  teamName: string;
  league: string;
}

export interface LeagueOptionDTO {
  code: string;
  label: string;
}

export interface TeamOptionDTO {
  code: string;
  label: string;
}